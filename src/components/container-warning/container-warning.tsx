import styled from "styled-components";
import { colors } from "../typography/colors";

export const ContainerWarning = styled.div`
  background-color: ${colors.stateswarningLight};
  padding: 8px 12px 8px 12px;
  border-radius: 5px;
`;
